interface:
  display_name: "Lumora"
  short_description: "Compose sites from exact section prompts"
  default_prompt: "Use $lumora to build a website from multiple different existing JSON section prompt_text entries 1:1, adapting only visible copy to the company context."
