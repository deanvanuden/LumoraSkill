interface:
  display_name: "Lumora"
  short_description: "Strict prompt-locked websites"
  default_prompt: "Use $lumora to scrape the target business, preserve multipage structure, load and hash every selected JSON prompt_text before coding, avoid banned prompt IDs, use original source hero/banner media and logo, prepare fitting sharp card media, keep design/layout/components/motion 100% 1:1, forbid bypassing any rule, and verify manifest, media plan, prompt fidelity, responsiveness, and working links."
