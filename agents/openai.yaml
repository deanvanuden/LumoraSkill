interface:
  display_name: "Lumora"
  short_description: "Scrape-first complete prompt sites"
  default_prompt: "Use $lumora to scrape the target business, create a niche section blueprint, then build a complete website from exact JSON prompt_text entries 1:1 while adapting only visible copy."
