interface:
  display_name: "/lumora"
  short_description: "Image-first static websites"
  default_prompt: "Use $lumora to plan a premium static website from the section library, generate or inspect a visual reference first, then assemble plain HTML/CSS/JS pages with selected MotionSites-derived sections, cohesive styling, fit-for-slot media, working routes/interactions, and desktop/mobile verification."
