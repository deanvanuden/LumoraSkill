interface:
  display_name: "Lumora"
  short_description: "Strict 1:1 prompt websites"
  default_prompt: "Use $lumora to scrape the target business, create a niche section blueprint, then build a complete website from exact JSON prompt_text entries 100% 1:1 with no adaptation except permitted section background changes."
