interface:
  display_name: "Lumora"
  short_description: "Build locked websites from prompt entries"
  default_prompt: "Use $lumora to build a website from existing JSON prompt entries, adapting only visible copy to the company context."
