interface:
  display_name: "Lumora"
  short_description: "Build from exact prompt_text entries"
  default_prompt: "Use $lumora to build a website from existing JSON prompt_text entries 1:1, adapting only visible copy to the company context."
