interface:
  display_name: "Lumora"
  short_description: "Compose sites from exact prompts"
  default_prompt: "Use $lumora to build a website from exact JSON prompt_text entries 1:1, using a complete page base or diverse added sections while adapting only visible copy."
